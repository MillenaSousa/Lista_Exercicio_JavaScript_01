function area(){
    var base = parseFloat(prompt("Digite o valor da base: "));
    var altura = parseFloat(prompt("Digite o valor da area: "));
    area = (altura * base) / 2 
    alert("O valor da area do triangulo é " + area)
}